// the first element of the priority queue is alaway the greatest
// default priority queue is alawys a max heap 
// on fetching the element that comes out is the alaways the greatest of them
//
#include<iostream>
#include<queue>
using namespace std;
int main() {
    priority_queue<int> p;
    
    return 0;
}